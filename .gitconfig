[user]
	name = alvarogaroconstella
	email = alvaro.garcia@constellaintelligence.com
[alias]
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    yolo = !git commit -m \"$(curl -L whatthecommit.com/index.txt)\"
[core]
	editor = vim
[credential]
	helper = store
